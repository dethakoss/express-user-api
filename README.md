# express-user-api
 A simple express user api with mysql
#Requirements
Mysql
Nodejs

#Installation
npm install
npm run dev

#mysql table
create table users(
   id varchar(50) NOT NULL,
   name VARCHAR(50) NOT NULL,
   PRIMARY KEY (id)
);
